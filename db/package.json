{
  "name": "ch-pipeline-db",
  "version": "0.1.0",
  "private": true,
  "description": "Schema + seed for the CH Business Services lead pipeline (Neon Postgres)",
  "scripts": {
    "seed": "node seed.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
