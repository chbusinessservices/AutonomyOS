{
  "name": "ch-pipeline-scripts",
  "version": "0.5.0",
  "private": true,
  "description": "CH pipeline scripts — Phase 2 lead extraction (Playwright Maps sweeps), Phase 3 LinkedIn outreach engine (capped, paced, dry-run-first), Phase 4 AI personalization + negotiation (DeepSeek, template-fallback-safe), Phase 5 Stripe payments arm (signed webhook → closed_won + payment-link helper), all writing to the Neon pipeline",
  "scripts": {
    "extract": "node extract-leads.js",
    "extract:dry": "node extract-leads.js --dry-run",
    "outreach": "node linkedin-outreach.js",
    "outreach:dry": "node linkedin-outreach.js --dry-run",
    "webhook": "node stripe-webhook.js",
    "payment:link": "node payment.js",
    "test": "node --test test/linkedin-outreach.test.js test/ai.test.js test/stripe.test.js test/extract-parser.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "playwright": "^1.49.0"
  }
}
